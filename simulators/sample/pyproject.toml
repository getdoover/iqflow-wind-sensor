[project]
name = "iqflow-wind-sensor-sim"
version = "0.1.0"
description = "Simulator that emits IQWS-shaped raw register values via tags."
requires-python = ">=3.11"
dependencies = [
    "pydoover>=1.0.0a8",
]

[project.scripts]
doover-app-run = "main:main"
